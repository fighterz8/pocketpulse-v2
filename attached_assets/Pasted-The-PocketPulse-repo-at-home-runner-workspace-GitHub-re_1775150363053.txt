The PocketPulse repo at /home/runner/workspace (GitHub remote: https://github.com/fighterz8/pocketpulse-v1.git) needs the following git operations to close out Phase 1 and prepare for Phase 2:

Step 1: Push the phase-1 branch. There are 6 unpushed commits on feature/phase-1-auth-account-setup. Push them to origin.

Step 2: Push main to origin. The local main branch exists at 79aa1e4 but has never been pushed to origin. Push it: git push -u origin main

Step 3: Change default branch on GitHub. Use gh repo edit --default-branch main to set main as the default branch on GitHub.

Step 4: Merge phase-1 into main. Merge feature/phase-1-auth-account-setup into main. The branches have diverged (main has 6 doc/image commits, phase-1 has 7 implementation commits), so this will be a merge commit, not a fast-forward. You can either:

Create a PR on GitHub with gh pr create from feature/phase-1-auth-account-setup into main, then merge it, OR
Merge locally: git checkout main && git merge feature/phase-1-auth-account-setup then push
The PR description (if using PR route) should be:

Title: Phase 1: Authentication and Account Setup

Body:

Implements session-based auth (register, login, logout) with PostgreSQL session store
Adds protected routing and first-account onboarding gate
Creates app shell with sidebar navigation (Dashboard, Upload, Ledger, Leaks placeholders)
Schema: users, user_preferences, accounts, session tables
API: /api/auth/register, /api/auth/login, /api/auth/logout, /api/auth/me, /api/accounts
Tests: auth, routes, schema, client app shell, useAuth hook
Requirement IDs: AU-01 through AU-04.1
Step 5: Create the Phase 2 branch. After main is updated with the merge, create a new branch from main: git checkout -b feature/phase-2-upload-import main && git push -u origin feature/phase-2-upload-import

Step 6: Verify. Confirm that main on GitHub contains the Phase 1 implementation code, and that feature/phase-2-upload-import exists on origin pointing to the same merge commit.